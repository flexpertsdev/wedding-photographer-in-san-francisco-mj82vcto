---
// Hero.astro
const { heroImage, heroTitle, heroSubtitle } = Astro.props;
---

<section class="relative bg-gray-100 py-24">
  <div class="absolute inset-0">
    <img
      src={heroImage}
      alt="Hero Image"
      class="object-cover w-full h-full opacity-75"
      loading="lazy"
    />
    <div class="absolute inset-0 bg-black opacity-20"></div> {/* Dark overlay for text readability */}
  </div>

  <div class="relative container mx-auto text-center">
    <h1 class="text-4xl md:text-6xl font-bold text-white mb-4 drop-shadow-md">
      {heroTitle}
    </h1>
    <p class="text-lg md:text-xl text-gray-200 mb-8 drop-shadow-md">
      {heroSubtitle}
    </p>
    <a href="/contact" class="bg-primary text-white py-3 px-6 rounded-md hover:bg-primary-dark transition-colors duration-200 inline-block">
      Get in Touch
    </a>
  </div>
</section>

<style>
  .bg-primary {
    background-color: theme('colors.rose.600'); /* Example: Using a custom color from Tailwind config */
  }

  .bg-primary-dark {
    background-color: theme('colors.rose.700');
  }
</style>